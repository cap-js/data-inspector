meta {
  name: GET EntityDefinitions /$count with $filter
  type: http
  seq: 12
}

get {
  url: http://localhost:4004/odata/v4/data-inspector/EntityDefinition/$count?$filter=contains(name, 'Product')
  body: none
  auth: basic
}

params:query {
  $filter: contains(name, 'Product')
}

auth:basic {
  username: alice
}
