meta {
  name: GET EntityDefinitions $filter eq elements
  type: http
  seq: 19
}

get {
  url: http://localhost:4004/odata/v4/data-inspector/EntityDefinition?$filter=elements eq 'table'&$count=true
  body: none
  auth: basic
}

params:query {
  $filter: elements eq 'table'
  $count: true
}

auth:basic {
  username: alice
}
