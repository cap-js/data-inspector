meta {
  name: GET EntityDefinitions $select title
  type: http
  seq: 30
}

get {
  url: http://localhost:4004/odata/v4/data-inspector/EntityDefinition?$select=title
  body: none
  auth: basic
}

params:query {
  $select: title
}

auth:basic {
  username: alice
}
