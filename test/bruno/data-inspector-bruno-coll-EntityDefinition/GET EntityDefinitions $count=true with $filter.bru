meta {
  name: GET EntityDefinitions $count=true with $filter
  type: http
  seq: 11
}

get {
  url: http://localhost:4004/odata/v4/data-inspector/EntityDefinition?$count=true&$filter=contains(name, 'Product')&$select=name
  body: none
  auth: basic
}

params:query {
  $count: true
  $filter: contains(name, 'Product')
  $select: name
}

auth:basic {
  username: alice
}
