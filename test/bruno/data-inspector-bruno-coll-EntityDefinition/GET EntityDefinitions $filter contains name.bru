meta {
  name: GET EntityDefinitions $filter contains name
  type: http
  seq: 20
}

get {
  url: http://localhost:4004/odata/v4/data-inspector/EntityDefinition?$filter=contains(name, 'Pro')&$select=title,name&$count=true
  body: none
  auth: basic
}

params:query {
  $filter: contains(name, 'Pro')
  $select: title,name
  $count: true
}

auth:basic {
  username: alice
}
