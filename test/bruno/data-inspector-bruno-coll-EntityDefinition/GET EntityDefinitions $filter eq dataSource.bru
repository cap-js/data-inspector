meta {
  name: GET EntityDefinitions $filter eq dataSource
  type: http
  seq: 15
}

get {
  url: http://localhost:4004/odata/v4/data-inspector/EntityDefinition?$filter=dataSource eq 'service'&$select=name&$count=true
  body: none
  auth: basic
}

params:query {
  $filter: dataSource eq 'service'
  $select: name
  $count: true
}

auth:basic {
  username: alice
}
