meta {
  name: GET EntityDefinitions $filter eq type
  type: http
  seq: 18
}

get {
  url: http://localhost:4004/odata/v4/data-inspector/EntityDefinition?$filter=type eq 'projection'&$select=name&$count=true
  body: none
  auth: basic
}

params:query {
  $filter: type eq 'projection'
  $select: name
  $count: true
}

auth:basic {
  username: alice
}
