meta {
  name: GET Data service
  type: http
  seq: 12
}

get {
  url: http://localhost:4004/odata/v4/data-inspector/Data?$filter=entityName eq 'OrderService.Order'
  body: none
  auth: basic
}

params:query {
  $filter: entityName eq 'OrderService.Order'
}

auth:basic {
  username: alice
}
