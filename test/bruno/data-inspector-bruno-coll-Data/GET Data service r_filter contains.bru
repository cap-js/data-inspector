meta {
  name: GET Data service r_filter contains
  type: http
  seq: 18
}

get {
  url: http://localhost:4004/odata/v4/data-inspector/Data?$filter=entityName eq 'datainspector.test.srv.ProductService.Product'&r_select=description,productName&r_filter=contains(productName, 'Apple')
  body: none
  auth: basic
}

params:query {
  $filter: entityName eq 'datainspector.test.srv.ProductService.Product'
  r_select: description,productName
  r_filter: contains(productName, 'Apple')
}

auth:basic {
  username: alice
}
